# دليل إعداد النظام الكامل 📋

## 1️⃣ إعداد قاعدة البيانات في Supabase

### الخطوة الأولى: نفذ هذا SQL
افتح Supabase Dashboard → SQL Editor → New Query → الصق هذا الكود:

```sql
انسخ كل محتوى ملف COMPLETE_SETUP.sql وشغله
```

### الخطوة الثانية: تعطيل تأكيد البريد (مهم جداً!)
1. افتح **Authentication** → **Settings**
2. أسفل "Email Auth"
3. **ألغِ تفعيل** ☐ Confirm email
4. احفظ التغييرات

---

## 2️⃣ ما تم تحسينه في النظام

### ✅ التسجيل والتحقق:
- **البيانات المحفوظة**: الاسم، البريد، رقم الجوال، كلمة المرور
- **التحقق عبر Gmail**: يرسل رمز للبريد
- **التحقق عبر Telegram**: يتحقق من الرقم مباشرة
- **تسجيل تلقائي**: الـ Trigger ينشئ User Profile تلقائياً

### ✅ الطلبات:
- **بيانات العميل**: اسم العميل، رقم الجوال، العنوان
- **تفاصيل المنتجات**: اسم المنتج، الكمية، السعر
- **خيارات إضافية**: طريقة التقطيع، التغليف، ملاحظات
- **تتبع الحالة**: pending, processing, completed, cancelled

### ✅ جداول قاعدة البيانات:
```
users → البيانات الشخصية للمستخدمين
orders → جميع الطلبات مع بيانات العميل
order_items → عناصر كل طلب
products → المنتجات المتاحة
categories → الفئات
```

---

## 3️⃣ كيف يعمل النظام الآن

### 📝 التسجيل:
1. المستخدم يملأ البيانات (username, email, phone, password)
2. يختار Gmail أو Telegram
3. **Gmail**: يرسل OTP للبريد → يدخل الكود → يتم التسجيل
4. **Telegram**: ينشئ الحساب → يرسل رابط للبوت → يشارك رقمه → يتحقق تلقائياً

### 🛒 الطلبات:
1. المستخدم يضيف منتجات للسلة
2. يدخل العنوان وبياناته
3. يؤكد الطلب
4. **يتم حفظ**:
   - معلومات العميل (الاسم، الرقم، العنوان)
   - تفاصيل كل منتج (الاسم، الكمية، السعر، التقطيع، التغليف)
   - الوقت والتاريخ
   - حالة الطلب
5. المستخدم يقدر يشوف طلباته في صفحة "طلباتي"

---

## 4️⃣ الأوامر المطلوبة

### تحديث قاعدة البيانات:
```bash
npm run db:push
```

### تشغيل السيرفر:
```bash
npm run dev
```

---

## 5️⃣ اختبار النظام

### اختبار التسجيل:
1. افتح الموقع على http://localhost:5000
2. اضغط "حساب جديد"
3. املأ البيانات
4. جرب Gmail أو Telegram
5. تأكد من تسجيل الدخول بنجاح

### اختبار الطلبات:
1. سجل دخول
2. تصفح المنتجات
3. أضف منتجات للسلة
4. اذهب للسلة
5. أكمل الطلب
6. تحقق من صفحة "طلباتي" أن الطلب موجود

### التحقق من قاعدة البيانات:
افتح Supabase → Table Editor → تحقق من:
- **users**: هل البيانات محفوظة (username, email, phone)؟
- **orders**: هل الطلب موجود مع بيانات العميل؟
- **order_items**: هل عناصر الطلب محفوظة؟

---

## ✅ تم الإعداد بنجاح!

الآن النظام جاهز:
- ✅ التسجيل يحفظ كل البيانات
- ✅ التحقق عبر Gmail وTelegram يعمل
- ✅ الطلبات تُسجل بالكامل
- ✅ قاعدة البيانات منظمة ومحمية

**ملاحظة مهمة**: إذا واجهتك مشاكل في التسجيل، تأكد من:
1. تعطيل "Confirm email" في Supabase
2. تشغيل السيرفر بشكل صحيح
3. فحص Console للأخطاء

# حل مشكلة إضافة الموظفين في لوحة التحكم

## المشكلة 🔴
عند محاولة إضافة موظف جديد من لوحة التحكم (Admin Dashboard)، لا يتم حفظ البيانات ولا يظهر الموظف في القائمة.

## السبب 🔍
سياسات Row Level Security (RLS) على جدول `staff` تمنع الإدخال المباشر من backend API.

## الحل ✅

يوجد ملف SQL جديد: `FIX_STAFF_RLS.sql`

### الخطوات:

#### الطريقة 1: عبر Supabase Dashboard (الموصى بها)
1. افتح متصفحك واذهب إلى: https://supabase.com/dashboard
2. سجل الدخول لحسابك
3. اختر المشروع: **rfwnclhnxhatnybxjlhm**
4. من القائمة الجانبية، اختر **SQL Editor**
5. اضغط على **+ New Query**
6. انسخ والصق محتويات ملف `FIX_STAFF_RLS.sql`
7. اضغط **Run** أو **Ctrl+Enter**
8. يجب أن تظهر رسالة "Success. No rows returned"

#### الطريقة 2: عبر Supabase CLI (إذا كان مثبت)
```bash
supabase db execute --file FIX_STAFF_RLS.sql
```

### التحقق من نجاح الإصلاح:
1. بعد تشغيل SQL، ارجع للوحة التحكم في الموقع
2. اذهب إلى تبويب "الموظفين" (Staff)
3. اضغط "إضافة موظف جديد"
4. أدخل البيانات المطلوبة
5. اضغط "إضافة الموظف الآن"
6. يجب أن يظهر الموظف في القائمة مباشرة! ✅

### ملاحظة 📝
لا تحتاج لإعادة تشغيل السيرفر - التغييرات تطبق مباشرة على قاعدة البيانات.

### ما الذي يفعله السكربت؟
- يحذف السياسات القديمة المقيدة على `staff`
- يُنشئ سياسات جديدة تسمح بـ:
  - القراءة (SELECT) للجميع
  - الإدخال/التحديث/الحذف (INSERT/UPDATE/DELETE) من service role والمسؤولين
